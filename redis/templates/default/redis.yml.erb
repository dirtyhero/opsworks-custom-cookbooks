development:
  redis_namespace: <%= ENV["REDIS_NAMESPACE_PREFIX"] %>
  redis:
    host: <%= ENV["REDIS_HOST"] %>
    port: <%= ENV["REDIS_PORT"] || 6379 %>

test:
  redis_namespace: <%= ENV["REDIS_NAMESPACE_PREFIX"] %>
  redis:
    host: <%= ENV["REDIS_HOST"] %>
    port: <%= ENV["REDIS_PORT"] || 6379 %>

production:
  redis_namespace: <%= ENV["REDIS_NAMESPACE_PREFIX"] %>
  redis:
    host: <%= ENV["REDIS_HOST"] %>
    port: <%= ENV["REDIS_PORT"] || 6379 %>